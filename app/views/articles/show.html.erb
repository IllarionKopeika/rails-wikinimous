<div class="container">
  <div class="justify-content-center row">
    <div class="col-sm-8 col-sm-offset-2">
      <article>
        <h2><%= @article.title %></h2>
        <p><%= @article.content %></p>
      </article>
          <%= link_to "Edit", edit_article_path, class: "btn btn-outline-primary" %>
          <%= link_to "Delete", article_path(@article), class: "btn btn-outline-primary", data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
          <%= link_to "Back", articles_path, class: "btn btn-outline-primary" %>
    </div>
  </div>
</div>
